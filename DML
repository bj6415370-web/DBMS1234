CREATE DATABASE CollegeDB;

USE CollegeDB;

CREATE TABLE Student (
    Student_ID INT PRIMARY KEY,
    Student_Name VARCHAR(50),
    Course VARCHAR(30),
    Age INT,
    City VARCHAR(30)
);

INSERT INTO Student VALUES
(101, 'Amit', 'BCA', 20, 'Pune'),
(102, 'Sneha', 'BBA', 21, 'Mumbai'),
(103, 'Rohit', 'BSc', 19, 'Delhi'),
(104, 'Priya', 'BCA', 22, 'Nagpur');

SELECT * FROM Student;

UPDATE Student
SET City = 'Nashik'
WHERE Student_ID = 103;

SELECT * FROM Student;

DELETE FROM Student
WHERE Student_ID = 104;

SELECT * FROM Student;
